# Change-Detection-Blob-Matching
This notebook implements the change detection pipeline taught in the motion analysis class and uses the Otsu thresholding method from the image segmentation class as an alternative threshold selection rule.
